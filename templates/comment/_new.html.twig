          <!-- Comment Form -->
          <div class="post__comment-form d-flex flex-column flex-lg-row align-items-center gap-4">
            <img src="{{ app.user.avatarLink }}" class="img-thumbnail rounded-circle col-7 col-sm-4 col-md-3 col-lg-2 col-xl-1" alt="...">
            <div class="card shadow-sm w-100">
              <div class="card-body">
                <form action="{{ path('app_comment_new')}}" method="post">
                  <div class="mb-3">
                    <label for="exampleFormControlTextarea1" class="form-label"><span class="fw-bold">{{ app.user.username }}</span>, postez votre commentaire !</label>
                    <textarea class="form-control" id="exampleFormControlTextarea1" rows="3" placeholder="Article très intéressant !"></textarea>
                  </div>
                  <button class="btn btn-primary float-end" type="submit">Poster votre commentaire</button>
                </form>
              </div>
            </div>
          </div>